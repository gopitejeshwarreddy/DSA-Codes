#include<iostream>
using namespace std;

int main(){
    // step 1: initialise before loop
    // step 2: increment inside the loop
    // step 3: condition with in the brackets of while loop

    int i = 1;
    do{
        cout<<"hellow world: "<<i<<endl;
        i++;
    }while(i<=10);


    return 0;
}
